import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.tatunino.com',
  appName: 'qrscanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
